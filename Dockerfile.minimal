FROM python:3.11-slim

WORKDIR /app

# Copy the simple server (no dependencies needed)
COPY simple-server.py .

# Set environment
ENV PORT=8080

EXPOSE 8080

# Run the simple server
CMD ["python", "simple-server.py"]
