FROM python:3.11-slim

WORKDIR /app

# Install only Flask
RUN pip install flask

# Copy the minimal wrapper
COPY minimal-wrapper.py .

# Set environment
ENV PORT=8080

EXPOSE 8080

# Run the minimal wrapper
CMD ["python", "minimal-wrapper.py"]
