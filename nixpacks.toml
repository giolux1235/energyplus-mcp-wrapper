[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-test.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python energyplus-robust-api.py"
